using Raylib_cs;
using System.Numerics;
public class Hunter()
{
    public static Texture2D texture;

    // public static void HunterIdleAnimation()
    // {
    //         if(!Raylib.IsKeyPressed(KeyboardKey.H))Texture.Huntersize(texture); 
    //         if(!Raylib.IsKeyPressed(KeyboardKey.J))Texture.Huntersize(texture); 
    //         if(!Raylib.IsKeyPressed(KeyboardKey.K))Texture.Huntersize(texture); 
    //         if(!Raylib.IsKeyPressed(KeyboardKey.L))Texture.Huntersize(texture); 
    //         Texture.Huntersize(texture);
    // }
    

}